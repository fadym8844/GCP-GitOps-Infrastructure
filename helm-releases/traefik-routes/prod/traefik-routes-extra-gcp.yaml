# Extra IngressRoutes for GCP - namespaces outside core-platform
# Exact mirror of live OCI IngressRoutes confirmed by kubectl get ingressroute -A -o yaml
# Apply after Traefik is running:
#   kubectl apply -f traefik-routes-extra-gcp.yaml

---
# ArgoCD - confirmed from live OCI: cd.core-platform.com → argocd-server:80
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes: 
    - kind: Rule
      match: Host(`argocd.core-platform.com`) && PathPrefix(`/`)
      services:
        - name: argocd-server
          port: 80
  tls:
    secretName: cd-traefik-cert

---
# Grafana - confirmed from live OCI: monitoring.core-platform.com → monitoring-grafana:80
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kube-prometheus-stack-grafana
  namespace: monitoring
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`monitoringgcp.core-platform.com`) && PathPrefix(`/`)
      services:
        - name: kube-prometheus-stack-grafana
          port: 80
  tls:
    secretName: traefik-cert-monitoring-prod

---
# RabbitMQ Management UI - confirmed from live OCI: rabbitmq.core-platform.com → core-platform:15672
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rabbitmq-ui
  namespace: rabbitmq-system
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`rabbitmqgcp.core-platform.com`) && PathPrefix(`/`)
      services:
        - name: core-platform
          port: 15672
  tls:
    secretName: rabbitmq-traefik-cert-prod
