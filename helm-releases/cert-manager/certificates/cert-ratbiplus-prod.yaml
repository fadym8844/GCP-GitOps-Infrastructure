apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: core-platform-ratbiplus-tls
  namespace: core-platform
spec:
  secretName: core-platform-ratbiplus-tls
  issuerRef:
    name: acme-http-issuer-traefik
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - ratbiplus.core-platform.com
 
