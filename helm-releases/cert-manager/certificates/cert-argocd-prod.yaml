apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cd-traefik-cert
  namespace: argocd
spec:
  dnsNames:
    - argocd.core-platform.com
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: acme-http-issuer-traefik
  secretName: cd-traefik-cert 
